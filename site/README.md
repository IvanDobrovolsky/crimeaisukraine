# Site — crimeaisukraine.org

Astro static site. 7 languages (EN, UK, CRH, DE, ES, FR, RU).

```bash
npm install
npm run dev       # localhost:4321
npm run build     # production build → dist/
```

Deployed via Cloudflare Pages.
