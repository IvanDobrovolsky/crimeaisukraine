[project]
name = "crimeaisukraine-media"
version = "1.0.0"
description = "Media framing audit (GDELT + LLM verification)"
requires-python = ">=3.10"
dependencies = ["requests>=2.31.0", "beautifulsoup4>=4.12.0", "google-cloud-bigquery>=3.0.0"]
